if(SIM)
  message("adding sim target")
  add_subdirectory(sim)
else()
 if(VST)
  message("adding vst target")
  add_subdirectory(plugin)
  else()
  message("adding embedded target")
  add_subdirectory(black-delay)
  endif()
endif()